)
res
resdt <- get_ENdt(res, type = imp_type)
imp_type
restt <- get_trusted(res)
res
data.table::rbindlist(lapply(res, function(pen) {
x <- pen$pen_trusted
x[, imptype := "trust"]
}))
res=as.list()
restt <- res$pen_trusted
respp <- res$pen_priors
restt
rest[,.N, by=.(eid)]
restt[,.N, by=.(eid)]
restt[,if(.N>1) .SD, by=.(eid)]
ani_impeggs[ani==2365]
ani_info[ani==2365]
cat("Imputing eggs: type = ", imp_type,"; for pens: ", pens)
plan(multisession)
pen_res_list <- future_lapply(pens, function(p) {
process_pen(
p, negg, meta, ani_info,
ignore_anis = ignore_anis,
index_floornest = index_floornest,
max_ani_pen = max_ani_pen,
ani_impeggs, weight_imd = weight_imd, factor_imd = factor_imd, p_perc_dups = p_perc_dups,
nearby_days = nearby_days, penalty_weight = penalty_weight, max_penalty = max_penalty,
weight_nl = weight_nl, factor_nl = factor_nl,
weight_np = weight_np, factor_np = factor_np,
norm_result = type,
flat = flat
)
})
plan(sequential)
restt <- get_trusted(pen_res_list)
if (nrow(restt[, .N, by = .(date, ani)][N>1]) > 0 ) {
warning("Check trusted dt from get_trusted(), dups for date/ani pair are detected!")
}
# print period
from_d <- as.character(min(restt$date))
to_d <- as.character(max(restt$date))
print(sprintf("Trusted results period: %s to %s", from_d, to_d))
restt[, .N, by = .(date, ani)][N>1]
restt[, if(.N>1) .SD, by = .(date, ani)]
dd="2023-09-20"
p=7
cat("Pen:", p, " \n")
pen_negg <- negg[Pen == p]
dates <- sort(unique(pen_negg$Date))
pen_meta <- meta[pen == p]
# Chickens preferred nest
ani_prefer_nest <- pen_meta[, .(sumdu = sum(Duration)), by = .(ani, Nestnumber)][ani != 0]
ani_prefer_nest[, prop := as.numeric(sumdu) / as.numeric(sum(sumdu)), by = ani]
ani_prefer_nest[is.na(prop), prop := 0]
pen_priors <- list()
pen_trusted <- list()
pen_rmegg <- list()
date <- as.Date(date)
# Pen/date egg count
pendate_negg <- pen_negg[Date == date]
pensurdate_negg <- pen_negg[Date %in% c(date-1, date+1)][order(Date)]
pendate_negg
date
=dd
date=d
date=dd
date <- as.Date(date)
# Pen/date egg count
pendate_negg <- pen_negg[Date == date]
pensurdate_negg <- pen_negg[Date %in% c(date-1, date+1)][order(Date)]
pendate_negg
pensurdate_negg <- pen_negg[Date %in% c(date-1, date+1)][order(Date)]
# Candidate animals in pen
check_date(ani_info$Leave)
ani_info[, Leave := as.Date(Leave)]
cand_ani <- ani_info[Pen == p & Leave >= date, ani]
# Sanity check: too many animals
if (length(cand_ani) > max_ani_pen) {
warning("Pen ", p, " has more than ",max_ani_pen," animals?")
}
# Check if all auto_ani in cand_ani
auto_ani <- unique(pen_meta$ani)
auto_ani <- auto_ani[!auto_ani %in% ignore_anis]
x <- auto_ani[!auto_ani %in% cand_ani]
if (length(x) != 0) {
if (nrow(pen_meta[ani %in% x & Date >= date]) > 0) {
warning("Animal ID(s) ", paste(x, collapse = ", "),
" from autonest not in ani_info, please check!")
}
}
# Build egg table
eggs <- pendate_negg[, .(Nest = c(rep(Nest, Nhand), rep(index_floornest, Nfloor[1])))]
eggs[, nestc := seq_len(.N), by = Nest]
eggs[, eggid := sprintf("%s%03d%02d", format(date, "%y%m%d"), Nest, nestc)]
eggs[, nestc := NULL]
from <- max(pendate_negg$date_time_pre)
to <- max(pendate_negg$date_time)
# Trusted eggs from autonest
dt_trust <- get_trusted_autonest(eggs, pen_meta, from, to)
dt_trust
cand_ani <- cand_ani[!cand_ani %in% dt_trust$ani]
# Looking backward and forward: Remove candi if it (had & will have) a trust record in the (prev & next) day
ani_tmp <- lapply(unique(pensurdate_negg$Date), function(d) {
negg_tmp <- pensurdate_negg[Date == d]
from_tmp <- max(negg_tmp$date_time_pre)
to_tmp <- max(negg_tmp$date_time)
eggs_tmp <- negg_tmp[, .(Nest = c(rep(Nest, Nhand), rep(index_floornest, Nfloor[1])))]
eggs_tmp[, nestc := seq_len(.N), by = Nest]
eggs_tmp[, eggid := sprintf("%s%03d%02d", format(date, "%y%m%d"), Nest, nestc)]
eggs_tmp[, nestc := NULL]
dt_trust_tmp <- get_trusted_autonest(eggs_tmp, pen_meta, from_tmp, to_tmp)
dt_trust_tmp$ani
})
cand_ani <- cand_ani[!cand_ani %in% Reduce(intersect, ani_tmp) ]
eggs <- eggs[!eggid %in% dt_trust$eid]
ndiff_eggcand <- nrow(eggs) - length(cand_ani)
ndiff_eggcand
# if more eggs than candidates, warning & remove extra egg(s)
if (ndiff_eggcand > 0) {
warning("Num.egg > Num.candidates! ",ndiff_eggcand," extra egg(s) for pen ",p," on ",date)
rmegg <- data.table(eggs[(length(cand_ani) + 1):nrow(eggs)],
date = date,
pen = p)
setnames(rmegg, "eggid", "eid")
pen_rmegg[[length(pen_rmegg) + 1]] <- rmegg
eggs <- eggs[1:length(cand_ani)]
if (length(cand_ani) == 0) eggs <- eggs[0]
}
# Naive prior
prior <- make_naive_prior(eggs$eggid, cand_ani)
if (flat == TRUE) {
prior <- prior
} else {
# Update by laydates (+imputed) based on laying pattern throughout the period
prior <- update_prior_laydates(prior, ani_impeggs, from, to, date, pen_meta,
weight = weight_imd, factor = factor_imd, p_perc_dups = p_perc_dups)
# Update by nearby days' laying pattern
prior <- update_prior_nestlay(prior, eggs, cand_ani, pen_meta, from, to,
index_floornest,
nearby_days = nearby_days,
penalty_weight = penalty_weight, max_penalty = max_penalty,
weight = weight_nl, factor = factor_nl)
# Update by nest preference
prior <- update_prior_nestpref(prior, eggs, ani_prefer_nest,
index_floornest, weight = weight_np, factor = factor_np)
# Normalise prior
prior <- Norm_prior(prior, result = norm_result, index_floornest, pen_meta)
}
prior
# Check prob sums
bad <- which(rowSums(prior) > (1 + 1e-6))
if (length(bad) > 0) {
warning("Date ", date, ": prob per egg (rowSums) > 1")
}
# Save per date results
if (nrow(prior) > 0) {
pen_priors[[length(pen_priors) + 1]] <- prior
}
if (nrow(dt_trust) > 0) {
pen_trusted[[length(pen_trusted) + 1]] <- data.table(dt_trust, pen = p)
}
data.table(dt_trust, pen = p)
devtools::install_github("ChiMeiSun/EggImpute", upgrade = "never")
devtools::install_github("ChiMeiSun/EggImpute", upgrade = "never")
library(EggImpute)
library(data.table)
library(future.apply)
library(readxl)
library(ggplot2)
library(patchwork)
library(hms)
rm(pen_res_list)
cat("Imputing eggs: type = ", imp_type,"; for pens: ", pens)
plan(multisession)
pen_res_list <- future_lapply(pens, function(p) {
process_pen(
p, negg, meta, ani_info,
ignore_anis = ignore_anis,
index_floornest = index_floornest,
max_ani_pen = max_ani_pen,
ani_impeggs, weight_imd = weight_imd, factor_imd = factor_imd, p_perc_dups = p_perc_dups,
nearby_days = nearby_days, penalty_weight = penalty_weight, max_penalty = max_penalty,
weight_nl = weight_nl, factor_nl = factor_nl,
weight_np = weight_np, factor_np = factor_np,
norm_result = type,
flat = flat
)
})
plan(sequential)
# ----- EN: trust -----
#trusted
restt <- get_trusted(pen_res_list)
if (nrow(restt[, .N, by = .(date, ani)][N>1]) > 0 ) {
warning("Check trusted dt from get_trusted(), dups for date/ani pair are detected!")
}
check <- restt[, if(.N > 1) .SD, by = .(date, ani)]
check
# ----- EN: dt -----
resdt <- get_ENdt(pen_res_list, type = imp_type)
## example of dup dates for an ani
# tmp <- resdt[, .N, by = .(eiddate, ani)][N>1]
# ridx <- sample(1:nrow(tmp), 1)
# td <- tmp[ridx, eiddate]
# ta <- tmp[ridx, ani]
# resdt[eiddate == td & ani == ta]
# Merge with info
resdt <- merge(ani_info, resdt,
by = "ani",
all = TRUE)
if (nrow(resdt[is.na(AnimalID)]) > 0 ) warning("Unmatched ani without AnimalID detected!")
resdt[, pen := NULL]
resdt[, diff := NULL]
cat("animals that do not have any egg assignment: \n")
resdt[is.na(prior), .N, by = .(Breed)]
resdt[is.na(date), date := as.Date(eiddate, format = "%y%m%d")]
# check Birth
check <- resdt[is.na(Birth), .N, by = .(ani, Breed, Pen, Leave, Genotype, AnimalID)]
if (nrow(check) > 0) {
message("Animals without Birth (not genotyped and leave early): \n")
print(check)
mb <- resdt[, .N, by = Birth][order(-N), Birth[1]]
message("set their Birth to ",mb," (majority Birth)")
resdt[is.na(Birth), Birth := mb]
ani_info[is.na(Birth), Birth := mb]
}
from_date <- as.Date(min(resdt$date, na.rm = TRUE))
to_date <- as.Date(max(resdt$date, na.rm = TRUE))
#rm animals left too early
resdt <- resdt[! (is.na(prior) & Leave < from_date) ]
# add dates for animals with max assignment date < Leave (till measurement date "to_date" if Leave > to_date)
const_cols <- setdiff(colnames(resdt), c("date", "eiddate", "prior", "eid", "laytime", "type", "imptype"))
all_dates <- resdt[, .(date = seq(from_date, pmin(Leave, to_date), by = "day")), by = const_cols]
resdt <- merge(all_dates, resdt, by = c(const_cols, "date"), all = TRUE)
resdt[is.na(prior), prior := 0]
resdt <- resdt[!is.na(date)] # rm NA rows for animals that do not have any egg assignment
# resdt[AnimalID=="RH1731"] # Leave > to_date
# resdt[AnimalID=="RH1825"] # Leave < to_date
# resdt[AnimalID=="RH2101"] # not in assign
# resdt[AnimalID=="RH2266"] # no assignment
#
resdt[, week := as.integer((difftime(date, Birth, units  = "weeks")))]
# print weeks
from_w <- as.integer(min(resdt$week))
to_w <- as.integer(max(resdt$week))
print(sprintf("Period: from %s weeks to %s weeks",from_w,to_w))
resdt[, wgrp := floor((week - from_w) / EN_period_perweek) + 1]
print(sprintf("Period: from EN%s to EN%s",min(resdt$wgrp),max(resdt$wgrp)))
resdt[, min_wgrp := min(wgrp), by = AnimalID]
resdt[, max_wgrp := max(wgrp), by = AnimalID]
table(resdt$min_wgrp)
table(resdt$max_wgrp)
check <- resdt[imptype == "trust", if(.N >1) .SD, by = .(AnimalID, date)]
if (nrow(check) > 0 ){
message("Trust records were repeated, please check!")
print(check)
}
check <- resdt[!is.na(eid), if(.N >1) .SD, by = .(eid)]
check
#imp assign
respp <- get_prob(pen_res_list)
check <- restt[, if(.N > 1) .SD, by = .(eid)]
check
check <- respp[, if(.N > 1) .SD, by = .(eid)]
if (nrow(check) > 0 ) {
warning("Check trusted dt, dups for date/ani pair are detected!")
print(check)
}
check[pen==7]
p
date="2023-05-01"
cat("Pen:", p, " \n")
pen_negg <- negg[Pen == p]
dates <- sort(unique(pen_negg$Date))
pen_meta <- meta[pen == p]
check[eid=="23050109901"]
ani_prefer_nest <- pen_meta[, .(sumdu = sum(Duration)), by = .(ani, Nestnumber)][ani != 0]
ani_prefer_nest[, prop := as.numeric(sumdu) / as.numeric(sum(sumdu)), by = ani]
ani_prefer_nest[is.na(prop), prop := 0]
pen_priors <- list()
pen_trusted <- list()
pen_rmegg <- list()
date <- as.Date(date)
date
pendate_negg <- pen_negg[Date == date]
pensurdate_negg <- pen_negg[Date %in% c(date-1, date+1)][order(Date)]
pendate_negg
check_date(ani_info$Leave)
ani_info[, Leave := as.Date(Leave)]
cand_ani <- ani_info[Pen == p & Leave >= date, ani]
# Sanity check: too many animals
if (length(cand_ani) > max_ani_pen) {
warning("Pen ", p, " has more than ",max_ani_pen," animals?")
}
# Check if all auto_ani in cand_ani
auto_ani <- unique(pen_meta$ani)
auto_ani <- auto_ani[!auto_ani %in% ignore_anis]
x <- auto_ani[!auto_ani %in% cand_ani]
if (length(x) != 0) {
if (nrow(pen_meta[ani %in% x & Date >= date]) > 0) {
warning("Animal ID(s) ", paste(x, collapse = ", "),
" from autonest not in ani_info, please check!")
}
}
# Build egg table
eggs <- pendate_negg[, .(Nest = c(rep(Nest, Nhand), rep(index_floornest, Nfloor[1])))]
eggs[, nestc := seq_len(.N), by = Nest]
eggs[, eggid := sprintf("%s%03d%02d", format(date, "%y%m%d"), Nest, nestc)]
eggs[, nestc := NULL]
from <- max(pendate_negg$date_time_pre)
to <- max(pendate_negg$date_time)
# Trusted eggs from autonest
dt_trust <- get_trusted_autonest(eggs, pen_meta, from, to)
cand_ani
# Remove trusted animals/eggs from candidates
cand_ani <- cand_ani[!cand_ani %in% dt_trust$ani]
cand_ani
dt_trust
eggs
eggs
eggs[, nestc := seq_len(.N), by = Nest]
eggs[, eggid := sprintf("%s%03d%02d", format(date, "%y%m%d"), Nest, nestc)]
eggs[, nestc := NULL]
eggs
pendate_negg
from <- max(pendate_negg$date_time_pre)
to <- max(pendate_negg$date_time)
# Trusted eggs from autonest
dt_trust <- get_trusted_autonest(eggs, pen_meta, from, to)
# Remove trusted animals/eggs from candidates
cand_ani <- cand_ani[!cand_ani %in% dt_trust$ani]
# Looking backward and forward: Remove candi if it (had & will have) a trust record in the (prev & next) day
ani_tmp <- lapply(unique(pensurdate_negg$Date), function(d) {
negg_tmp <- pensurdate_negg[Date == d]
from_tmp <- max(negg_tmp$date_time_pre)
to_tmp <- max(negg_tmp$date_time)
eggs_tmp <- negg_tmp[, .(Nest = c(rep(Nest, Nhand), rep(index_floornest, Nfloor[1])))]
eggs_tmp[, nestc := seq_len(.N), by = Nest]
eggs_tmp[, eggid := sprintf("%s%03d%02d", format(date, "%y%m%d"), Nest, nestc)]
eggs_tmp[, nestc := NULL]
dt_trust_tmp <- get_trusted_autonest(eggs_tmp, pen_meta, from_tmp, to_tmp)
dt_trust_tmp$ani
})
Reduce(intersect, ani_tmp)
cand_ani
cand_ani <- cand_ani[!cand_ani %in% Reduce(intersect, ani_tmp) ]
cand_ani
eggs <- eggs[!eggid %in% dt_trust$eid]
ndiff_eggcand <- nrow(eggs) - length(cand_ani)
eggs
ndiff_eggcand
# Naive prior
prior <- make_naive_prior(eggs$eggid, cand_ani)
# Update by laydates (+imputed) based on laying pattern throughout the period
prior <- update_prior_laydates(prior, ani_impeggs, from, to, date, pen_meta,
weight = weight_imd, factor = factor_imd, p_perc_dups = p_perc_dups)
# Update by nearby days' laying pattern
prior <- update_prior_nestlay(prior, eggs, cand_ani, pen_meta, from, to,
index_floornest,
nearby_days = nearby_days,
penalty_weight = penalty_weight, max_penalty = max_penalty,
weight = weight_nl, factor = factor_nl)
# Update by nest preference
prior <- update_prior_nestpref(prior, eggs, ani_prefer_nest,
index_floornest, weight = weight_np, factor = factor_np)
prior
# Normalise prior
prior <- Norm_prior(prior, result = norm_result, index_floornest, pen_meta)
prior
# Check prob sums
bad <- which(rowSums(prior) > (1 + 1e-6))
if (length(bad) > 0) {
warning("Date ", date, ": prob per egg (rowSums) > 1")
}
# Save per date results
if (nrow(prior) > 0) {
pen_priors[[length(pen_priors) + 1]] <- prior
}
if (nrow(dt_trust) > 0) {
pen_trusted[[length(pen_trusted) + 1]] <- data.table(dt_trust, pen = p)
}
prior
dat=pen_res_list[[7]]
dat
dat=dat
$pen_priors
dat=dat$pen_priors
dat
dat[eid=="23050109901"]
get_prob <- function(res_list) {
data.table::rbindlist(lapply(res_list, function(pen) {
x <- pen$pen_priors
x[, imptype := "imp"]
}))
}
#imp assign
respp <- get_prob(pen_res_list)
check <- respp[, if(.N > 1) .SD, by = .(eid)]
respp
check <- respp[, if(.N > 1) .SD, by = .(eid, pen)]
if (nrow(check) > 0 ) {
warning("Check trusted dt, dups for date/ani pair are detected!")
print(check)
}
# ----- EN: dt -----
resdt <- get_ENdt(pen_res_list, type = imp_type)
## example of dup dates for an ani
# tmp <- resdt[, .N, by = .(eiddate, ani)][N>1]
# ridx <- sample(1:nrow(tmp), 1)
# td <- tmp[ridx, eiddate]
# ta <- tmp[ridx, ani]
# resdt[eiddate == td & ani == ta]
# Merge with info
resdt <- merge(ani_info, resdt,
by = "ani",
all = TRUE)
if (nrow(resdt[is.na(AnimalID)]) > 0 ) warning("Unmatched ani without AnimalID detected!")
resdt[, pen := NULL]
resdt[, diff := NULL]
cat("animals that do not have any egg assignment: \n")
resdt[is.na(prior), .N, by = .(Breed)]
resdt[is.na(date), date := as.Date(eiddate, format = "%y%m%d")]
# check Birth
check <- resdt[is.na(Birth), .N, by = .(ani, Breed, Pen, Leave, Genotype, AnimalID)]
if (nrow(check) > 0) {
message("Animals without Birth (not genotyped and leave early): \n")
print(check)
mb <- resdt[, .N, by = Birth][order(-N), Birth[1]]
message("set their Birth to ",mb," (majority Birth)")
resdt[is.na(Birth), Birth := mb]
ani_info[is.na(Birth), Birth := mb]
}
from_date <- as.Date(min(resdt$date, na.rm = TRUE))
to_date <- as.Date(max(resdt$date, na.rm = TRUE))
#rm animals left too early
resdt <- resdt[! (is.na(prior) & Leave < from_date) ]
# add dates for animals with max assignment date < Leave (till measurement date "to_date" if Leave > to_date)
const_cols <- setdiff(colnames(resdt), c("date", "eiddate", "prior", "eid", "laytime", "type", "imptype"))
all_dates <- resdt[, .(date = seq(from_date, pmin(Leave, to_date), by = "day")), by = const_cols]
resdt <- merge(all_dates, resdt, by = c(const_cols, "date"), all = TRUE)
resdt[is.na(prior), prior := 0]
resdt <- resdt[!is.na(date)] # rm NA rows for animals that do not have any egg assignment
# resdt[AnimalID=="RH1731"] # Leave > to_date
# resdt[AnimalID=="RH1825"] # Leave < to_date
# resdt[AnimalID=="RH2101"] # not in assign
# resdt[AnimalID=="RH2266"] # no assignment
#
resdt[, week := as.integer((difftime(date, Birth, units  = "weeks")))]
# print weeks
from_w <- as.integer(min(resdt$week))
to_w <- as.integer(max(resdt$week))
print(sprintf("Period: from %s weeks to %s weeks",from_w,to_w))
resdt[, wgrp := floor((week - from_w) / EN_period_perweek) + 1]
print(sprintf("Period: from EN%s to EN%s",min(resdt$wgrp),max(resdt$wgrp)))
resdt[, min_wgrp := min(wgrp), by = AnimalID]
resdt[, max_wgrp := max(wgrp), by = AnimalID]
table(resdt$min_wgrp)
table(resdt$max_wgrp)
resdt
check <- resdt[imptype == "trust", if(.N >1) .SD, by = .(AnimalID, date)]
if (nrow(check) > 0 ){
message("Trust records were repeated, please check!")
print(check)
}
check <- resdt[!is.na(eid), if(.N > 1) .SD, by = .(eid, Pen)]
check
check <- resdt[!is.na(eiddate), if(.N >1) .SD, by = .(AnimalID, eiddate)]
if (nrow(check) > 0 ){
message("Trust records were repeated, please check!")
print(check)
}
check[Pen==7]
resdt[imptype == "trust", if(.N >1) .SD, by = .(AnimalID, date)]
resdt[, if(.N >1) .SD, by = .(AnimalID, date)]
check <- resdt[!is.na(eid), if(.N > 1) .SD, by = .(eid, Pen)]
if (nrow(check) > 0 ){
message("Egg ids were repeated, please check!")
print(check)
}
View(resdt)
check <- resdt[!is.na(eiddate), if(.N > 1) .SD, by = .(AnimalID, eiddate, imptype)]
check
negg[Pen == 14 & Date=="2023-07-17"]
check <- resdt[!is.na(eid), if(.N > 1) .SD, by = .(eid, Pen)]
if (nrow(check) > 0 ){
message("Egg ids were repeated, please check!")
print(check)
}
resdt[, .N, by = .(AnimalID, date)]
resdt[, .N, by = .(AnimalID, date)][N > 1, .N]
resdt[, .N, by = .(AnimalID, eiddate)][N > 1, .N]
setnames(resdt, "prior", "egg")
resdt[, eiddate := as.Date(eiddate, format = "%y%m%d")]
setnames(resdt, "eiddate", "collectdate")
resdt[, -c("ani", "Genotype","eid", "KM", "KM_alt")]
View(info)
# # ----- args to set: input/output/Specific parameters -----
p_info <- "K:/LabWei/RegioHuhn_Projekt/RegioHuhn_ChiMeiSun/MetaData/2026-07-03_RegioHuhn_Info_Animals_SCM.txt"
# ----- Info: read, prepare -----
info <- fread(p_info, header = TRUE)
View(info)
devtools::install_github("ChiMeiSun/EggImpute", upgrade = "never")
devtools::install_github("ChiMeiSun/EggImpute", upgrade = "never")
devtools::install_github("ChiMeiSun/EggImpute", upgrade = "never")
remove.packages("EggImpute")
devtools::install_github("ChiMeiSun/EggImpute", upgrade = "never")
devtools::document()
setwd("K:/LabWei/RegioHuhn_Projekt/RegioHuhn_ChiMeiSun/EggImpute/EggImpute")
devtools::document()
devtools::install()
devtools::install_github("ChiMeiSun/EggImpute", upgrade = "never")
